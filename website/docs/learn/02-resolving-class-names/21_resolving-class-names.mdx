import { CodeEditor } from '@site/src/components/CodeEditor'
import _Resolving from '!!raw-loader!./_Resolving'

# Resolving class names

Resolving is a process when complex class name values are transformed back to final strings
that can be passed down to any `HTMLElement`s your components use under the hood.

<CodeEditor live>{_Resolving}</CodeEditor>
